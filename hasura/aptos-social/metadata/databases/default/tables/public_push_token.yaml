table:
  name: push_token
  schema: public
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - created_at
        - device_id
        - id
        - is_active
        - push_token
        - updated_at
        - user_id
select_permissions:
  - role: user
    permission:
      columns:
        - created_at
        - device_id
        - id
        - is_active
        - push_token
        - updated_at
        - user_id
      filter: {}
update_permissions:
  - role: user
    permission:
      columns:
        - created_at
        - device_id
        - id
        - is_active
        - push_token
        - updated_at
        - user_id
      filter: {}
      check: null
delete_permissions:
  - role: user
    permission:
      filter: {}
